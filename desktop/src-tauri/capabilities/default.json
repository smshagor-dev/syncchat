{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Native capabilities required by the local SyncChat desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "deep-link:default",
    "notification:default",
    "opener:default"
  ]
}
