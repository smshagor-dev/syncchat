import 'package:flutter/material.dart';

import 'live_device_contacts_screen.dart';

class LiveP0ContactsScreen extends StatelessWidget {
  const LiveP0ContactsScreen({super.key});

  @override
  Widget build(BuildContext context) => const LiveDeviceContactsScreen();
}
